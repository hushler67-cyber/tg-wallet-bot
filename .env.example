# --- Main user-facing bot ---
BOT_TOKEN=8926522121:AAGYVWmcoND5SptjUQtAmp88WIp8jgB2qxA

# --- Encryption ---
# 32-byte key, base64-encoded. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# NEVER commit this. Losing it means every stored private key becomes unrecoverable.
# Rotating it means re-encrypting the whole database.
MASTER_KEY=d1VJpiP+78TIfNxzOsRgciGAHjYCENW1+QVtMX71Mhs=

# --- Admin/backend bot (receives ADDRESSES ONLY, never private keys) ---
ADMIN_BOT_TOKEN=8642091522:AAFrC3hvf4S0DalDO2pHipfuMV2_sff1gZA
ADMIN_CHAT_ID=-7900090337

# --- Database ---
DB_PATH=./data/wallets.db
